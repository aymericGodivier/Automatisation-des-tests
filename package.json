{
  "scripts": {
    "merge-reports": "mochawesome-merge cypress/reports/*.json > cypress/reports/merged-report.json && marge cypress/reports/merged-report.json -o cypress/reports",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^13.11.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
